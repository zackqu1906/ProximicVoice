# Convenience entry point for source checkouts. Dependency definitions live in
# pyproject.toml so there is only one canonical package configuration.
-e .[ring,asr-streaming-sensevoice,asr-funasr-nano,ui]
